{
  "name": "vscode-autopr",
  "displayName": "VSCode AutoPR",
  "description": "Generate commit messages using Claude AI",
  "version": "1.0.0",
  "repository": "https://github.com/morcen/vscode-autopr",
  "icon": "logo.png",
  "publisher": "morcen",
  "engines": {
    "vscode": "^1.94.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "autopr.generateCommitMessage",
        "title": "AutoPR: Generate Commit Message",
        "icon": "$(hubot)",
        "shortTitle": "Generate Commit"
      },
      {
        "command": "autopr.createPullRequest",
        "title": "AutoPR: Create Pull Request",
        "icon": "$(git-pull-request)",
        "shortTitle": "Create PR"
      }
    ],
    "menus": {
      "scm/title": [
        {
          "command": "autopr.generateCommitMessage",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "AutoPR",
      "properties": {
        "autopr.model": {
          "type": "string",
          "default": "claude-opus-4-6",
          "description": "Claude model to use for generating commit messages"
        }
      }
    },
    "keybindings": [
      {
        "command": "autopr.generateCommitMessage",
        "key": "ctrl+shift+g c",
        "mac": "cmd+shift+g c",
        "when": "scmFocus || editorFocus"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "node esbuild.js --production",
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "compile": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.94.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.2"
  }
}
